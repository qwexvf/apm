Update plugins to latest version matching constraints.

Run: `apm update $ARGUMENTS`

No arguments = update all. Specific plugin name = update just that one.
Show what changed (before/after versions if available).
