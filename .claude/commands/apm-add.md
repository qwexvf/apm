Add a plugin to the manifest and install it.

Run: `apm add $ARGUMENTS`

If no arguments given, ask the user for the plugin name (e.g. `owner/repo` or `owner/repo@v1.2.3`).
Show the output. If it succeeds, confirm what was added and to which scope.
