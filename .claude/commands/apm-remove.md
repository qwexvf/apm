Remove a plugin from the manifest and uninstall it.

Run: `apm remove $ARGUMENTS`

If no arguments given, first run `apm list` to show installed plugins, then ask which to remove.
Show the output and confirm removal.
